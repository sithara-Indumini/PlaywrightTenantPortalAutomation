# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Handling keyboard actions in playwright
- Location: C:\WorkDirectory\tests\Chapter02\07_KeyBoard_Actions_Test.spec.ts:3:5

# Error details

```
Error: page.goto: NS_ERROR_UNKNOWN_HOST
Call log:
  - navigating to "https://www.google.com/", waiting until "load"

    at C:\WorkDirectory\tests\Chapter02\07_KeyBoard_Actions_Test.spec.ts:5:16
    at C:\WorkDirectory\tests\Chapter02\07_KeyBoard_Actions_Test.spec.ts:4:14
```

# Page snapshot

```yaml
- heading "Hmm. We’re having trouble finding that site." [level=1]
- paragraph: We can’t connect to the server at www.google.com.
- paragraph
- strong: "If you entered the right address, you can:"
- list:
  - listitem: Try again later
  - listitem: Check your network connection
  - listitem: Check that Nightly has permission to access the web (you might be connected but behind a firewall)
- button "Try Again"
```

# Test source

```ts
   1 | import { test, expect } from '@playwright/test';
   2 |
   3 | test('Handling keyboard actions in playwright', async ({ page }) => {
   4 |   await test.step('Navigating to URL', async () => {
>  5 |     await page.goto('https://www.google.com/')
     |                ^ Error: page.goto: NS_ERROR_UNKNOWN_HOST
   6 |
   7 |     //Enter action from keyboard
   8 |     await page.getByLabel('Search', { exact: true }).first().click();
   9 |     await page.getByLabel('Search', { exact: true }).first().fill('Playwright by testers talk');
  10 |     await page.getByLabel('Search', { exact: true }).first().press('Enter');
  11 |
  12 |     // // Selecting and deleting from the keyboard
  13 |     // await page.getByLabel('Search', { exact: true }).first().click();
  14 |     // await page.getByLabel('Search', { exact: true }).first().fill('Playwright by testers talk');
  15 |     // await page.keyboard.press('Control+A')
  16 |     // await page.keyboard.press('Delete')
  17 |
  18 |     // //Press TAB and Enter
  19 |     // await page.getByLabel('Search', { exact: true }).first().click();
  20 |     // await page.keyboard.press('Tab');
  21 |     // await page.keyboard.press('Enter')
  22 |     // await page.waitForTimeout(2000);
  23 |
  24 |
  25 |   });
  26 | })
  27 |
```